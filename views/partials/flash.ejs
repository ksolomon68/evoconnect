<% if (locals.flash && locals.flash.type && locals.flash.message) { %>
  <div class="flash flash-<%= flash.type %>" role="alert" aria-live="polite">
    <span class="flash-message"><%= flash.message %></span>
    <button class="flash-close" aria-label="Dismiss">&times;</button>
  </div>
<% } %>
