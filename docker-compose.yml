version: '3.8'

services:
  db:
    image: mysql:8
    container_name: evoconnect_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password_123!
      MYSQL_DATABASE: u579331817_evoconnect
      MYSQL_USER: u579331817_evoconnect
      MYSQL_PASSWORD: 'EQ;ujtgC>p1'
    ports:
      - "3306:3306"
    volumes:
      - evoconnect_db_data:/var/lib/mysql

volumes:
  evoconnect_db_data:
